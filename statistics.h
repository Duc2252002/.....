#ifndef STATISTICS_H
#define STATISTICS_H

#include <cmath> // for sqrt

// Function prototypes for mean and variance
double mean(const std::vector<double>& numbers);
double variance(const std::vector<double>& numbers);

#endif // STATISTICS_H
